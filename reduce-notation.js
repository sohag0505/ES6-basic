// // reduce - total numbers of elements are addition
// const nums = [1, 2, 3, 4, 5];
// const total = nums.reduce((previous, current) => previous + current, 0);
// console.log(total);

const students = {
    name: 'abul',
    age: 30,
    class: 'ten',
    marks: {
        math: 78,
        bangla: 67,
        english: 78
    }
}
const result = students.marks.english;
console.log(result);